# 🏗️ Deployment Architecture

## Current Setup (Development)

```
┌─────────────────────────────────────────────────────────┐
│                    Your Computer                         │
│                                                          │
│  ┌──────────────┐         ┌──────────────┐             │
│  │   Frontend   │────────▶│   Backend    │             │
│  │ localhost:   │         │ localhost:   │             │
│  │    5173      │         │    5000      │             │
│  └──────────────┘         └──────┬───────┘             │
│                                   │                      │
│                           ┌───────▼───────┐             │
│                           │   PostgreSQL  │             │
│                           │   Database    │             │
│                           └───────────────┘             │
└─────────────────────────────────────────────────────────┘
```

---

## Production Setup (Recommended: Vercel + Render)

```
┌────────────────────────────────────────────────────────────────┐
│                         INTERNET                                │
└────────────────────────────────────────────────────────────────┘
                              │
                              │
                ┌─────────────┴─────────────┐
                │                           │
                ▼                           ▼
    ┌───────────────────┐       ┌───────────────────┐
    │     VERCEL        │       │      RENDER       │
    │   (Frontend)      │       │   (Backend + DB)  │
    │                   │       │                   │
    │  ┌─────────────┐  │       │  ┌─────────────┐ │
    │  │   React     │  │       │  │  Node.js    │ │
    │  │   + Vite    │──┼───────┼─▶│  + Express  │ │
    │  │             │  │ HTTPS │  │             │ │
    │  │   (Static)  │  │       │  │  (API)      │ │
    │  └─────────────┘  │       │  └──────┬──────┘ │
    │                   │       │         │        │
    │  Global CDN       │       │  ┌──────▼──────┐ │
    │  100GB/month      │       │  │ PostgreSQL  │ │
    │  FREE ✓           │       │  │  Database   │ │
    │                   │       │  │  1GB FREE ✓ │ │
    └───────────────────┘       │  └─────────────┘ │
                                │                   │
    ruralshores-portal          │  750 hrs/month    │
    .vercel.app                 │  FREE ✓           │
                                └───────────────────┘
                                ruralshores-backend
                                .onrender.com
```

---

## Data Flow

### User Request Flow

```
1. User visits: https://ruralshores-portal.vercel.app
                              │
                              ▼
2. Vercel serves React app (HTML, CSS, JS)
                              │
                              ▼
3. User interacts (login, view data, etc.)
                              │
                              ▼
4. Frontend makes API call to:
   https://ruralshores-backend.onrender.com/api/...
                              │
                              ▼
5. Render backend processes request
                              │
                              ▼
6. Backend queries PostgreSQL database
                              │
                              ▼
7. Database returns data
                              │
                              ▼
8. Backend sends JSON response
                              │
                              ▼
9. Frontend displays data to user
```

---

## Deployment Workflow

### Continuous Deployment

```
┌──────────────┐
│   You Code   │
│   Locally    │
└──────┬───────┘
       │
       │ git push
       ▼
┌──────────────┐
│   GitHub     │
│  Repository  │
└──────┬───────┘
       │
       ├─────────────────┬─────────────────┐
       │                 │                 │
       │ Auto Deploy     │ Auto Deploy     │
       ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   Vercel     │  │   Render     │  │   Render     │
│  (Frontend)  │  │  (Backend)   │  │  (Database)  │
└──────────────┘  └──────────────┘  └──────────────┘
       │                 │                 │
       │                 │                 │
       └─────────────────┴─────────────────┘
                         │
                         ▼
                  ┌──────────────┐
                  │  Live App!   │
                  └──────────────┘
```

Every time you push to GitHub, both Vercel and Render automatically deploy your changes!

---

## Environment Variables Flow

### Development (.env.local)

```
Frontend                    Backend
┌──────────────┐           ┌──────────────────────┐
│ VITE_API_URL │           │ DATABASE_URL         │
│ = localhost  │           │ = localhost postgres │
│   :5000      │           │ PORT = 5000          │
└──────────────┘           │ JWT_SECRET = dev     │
                           └──────────────────────┘
```

### Production

```
Frontend (Vercel)           Backend (Render)
┌──────────────┐           ┌──────────────────────┐
│ VITE_API_URL │           │ DATABASE_URL         │
│ = https://   │           │ = postgres://render  │
│   backend    │           │ PORT = 10000         │
│   .render    │           │ JWT_SECRET = prod    │
│   .com       │           │ NODE_ENV = production│
└──────────────┘           └──────────────────────┘
```

---

## Security Layers

```
┌────────────────────────────────────────────────┐
│              User's Browser                     │
└────────────────┬───────────────────────────────┘
                 │
                 │ HTTPS (SSL/TLS Encryption)
                 ▼
┌────────────────────────────────────────────────┐
│         Vercel CDN (Frontend)                  │
│  • DDoS Protection                             │
│  • SSL Certificate (Auto)                      │
└────────────────┬───────────────────────────────┘
                 │
                 │ HTTPS API Calls
                 ▼
┌────────────────────────────────────────────────┐
│         Render Backend                         │
│  • CORS Protection                             │
│  • JWT Authentication                          │
│  • Environment Variables (Encrypted)           │
└────────────────┬───────────────────────────────┘
                 │
                 │ Internal Network
                 ▼
┌────────────────────────────────────────────────┐
│         PostgreSQL Database                    │
│  • Not publicly accessible                     │
│  • Encrypted connections                       │
│  • Automatic backups                           │
└────────────────────────────────────────────────┘
```

---

## Scaling Path

### Current (Free Tier)

```
Users: ~1,000/month
Bandwidth: 100GB
Database: 1GB
Cost: $0/month
```

### Growth (Paid Tier)

```
Users: ~100,000/month
Bandwidth: Unlimited
Database: 10GB
Cost: ~$30/month
  - Vercel Pro: $20
  - Render: $7 (backend) + $7 (database)
```

### Enterprise

```
Users: Unlimited
Custom infrastructure
Dedicated resources
Cost: Custom pricing
```

---

## Monitoring & Logs

```
┌─────────────────────────────────────────────┐
│            Vercel Dashboard                  │
│  • Deployment logs                          │
│  • Analytics                                │
│  • Performance metrics                      │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│            Render Dashboard                  │
│  • Backend logs (real-time)                 │
│  • Database metrics                         │
│  • Health checks                            │
│  • Resource usage                           │
└─────────────────────────────────────────────┘
```

---

## Backup Strategy

```
┌──────────────┐
│   GitHub     │  ◀── Source code (version control)
└──────────────┘

┌──────────────┐
│   Render DB  │  ◀── Automatic daily backups (7 days)
└──────────────┘

┌──────────────┐
│   Manual     │  ◀── Export database before major changes
└──────────────┘
```

---

## Cost Breakdown (Free Tier)

```
Component         Provider    Cost      Limits
─────────────────────────────────────────────────
Frontend          Vercel      $0        100GB bandwidth
Backend           Render      $0        750 hours/month
Database          Render      $0        1GB storage
Domain (optional) Namecheap   ~$10/yr   Custom domain
─────────────────────────────────────────────────
TOTAL                         $0/month  (or $10/yr with domain)
```

---

## Alternative Architectures

### Option 2: All Render

```
┌────────────────────────────────────┐
│           RENDER                    │
│                                     │
│  ┌──────────┐    ┌──────────┐     │
│  │ Frontend │    │ Backend  │     │
│  │ (Static) │───▶│ (API)    │     │
│  └──────────┘    └────┬─────┘     │
│                       │            │
│                  ┌────▼─────┐     │
│                  │PostgreSQL│     │
│                  └──────────┘     │
└────────────────────────────────────┘
```

### Option 3: Vercel + Supabase

```
┌──────────┐         ┌────────────────┐
│  Vercel  │         │   Supabase     │
│          │         │                │
│ Frontend │────────▶│ PostgreSQL +   │
│          │         │ Auto-generated │
│          │         │ REST API       │
└──────────┘         └────────────────┘
```

---

This architecture ensures:
✅ High availability
✅ Automatic scaling
✅ Global distribution
✅ Zero cost (free tier)
✅ Easy maintenance
✅ Professional deployment
